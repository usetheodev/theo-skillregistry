# PostgreSQL connection (pgvector). Used by the API, worker, Drizzle and integration tests.
THEOSKILL_PG_URI=postgresql://theoskill:theoskill@localhost:5432/theoskill

# HTTP port for the API server.
PORT=8080
