# Release v0.1.0

**Date:** 2026-06-22
**Verdict:** PR_OPEN_AWAITING_APPROVAL
**Source review:** knowledge-base/reviews/m0-walking-skeleton-review-2026-06-22.md (READY_TO_MERGE)
**Plan:** knowledge-base/plans/m0-walking-skeleton-plan.md (milestone M0)
**Bump:** minor (auto-derived — `Added` non-empty, no major trigger)
**PR:** https://github.com/usetheodev/theo-skillregistry/pull/1
**Base:** main (bootstrapped at root commit ce4be4d for this first release)
**Head:** develop @ 518a30c (`chore(release): 0.1.0`)
**Merge commit:** pending (awaiting human approval)
**Tag:** v0.1.0 (pending — created on the merge commit after approval)
**GitHub release:** pending

## Status

First release of the repository. `main` was bootstrapped from the root commit and the
release PR `develop → main` carries the macro ROADMAP + the M0 walking skeleton. The chain
is paused at the human-approval gate (Unbreakable Rule 4 — no auto-merge).

## Pending post-merge steps (resume with `/release` after the PR merges)

1. Tag the merge commit with annotated `v0.1.0` and push.
2. Publish the GitHub release with the rendered notes.
3. Flip `ROADMAP.md` M0 `[ ]` → `[x]` (plan declares `milestone_id: M0`) and append the
   roadmap-run record (`knowledge-base/roadmap-runs/M0-2026-06-22.md`).

## Release notes

### Added
- PRD.md, README.md, ROADMAP.md (macro, 9 milestones), Google baseline + 7 SOTA references.
- M0 — Walking skeleton: monorepo pnpm (`@usetheo/skillregistry` + `-api`), Hono +
  pg-boss LRO + PostgreSQL/Drizzle, health/skills/operations endpoints, ordered graceful
  shutdown, validated by an E2E test against real Postgres.

### Fixed
- M0: deterministic resolution of concurrent duplicate-skillId creation (one created, rest failed).
- M0: enqueue failure marks the operation failed instead of leaving it orphaned in CREATING.
