# Release v0.2.0

**Date:** 2026-06-23
**Verdict:** PR_OPEN_AWAITING_APPROVAL
**Source review:** knowledge-base/reviews/m1-skill-model-validation-review-2026-06-23.md (READY_TO_MERGE)
**Plan:** knowledge-base/plans/m1-skill-model-validation-plan.md (milestone M1)
**Bump:** minor (auto-derived — `Added` non-empty, no major trigger)
**PR:** https://github.com/usetheodev/theo-skillregistry/pull/2
**Base:** main · **Head:** develop @ 06d1174 (`chore(release): 0.2.0`)
**Merge commit:** pending (awaiting human approval)
**Tag:** v0.2.0 (pending — created on the merge commit after approval)
**GitHub release:** pending

## Status

M1 release. The PR `develop → main` carries the M1 skill model + rigorous validation. The
chain is paused at the human-approval gate (Unbreakable Rule 4 — no auto-merge).

## Pending post-merge steps (resume with `/release` after the PR merges)

1. Tag the merge commit with annotated `v0.2.0` and push.
2. Publish the GitHub release with the rendered notes.
3. Flip `ROADMAP.md` M1 `[ ]` → `[x]` (plan declares `milestone_id: M1`) + roadmap-run record.

## Release notes (summary)

### Added
- M1 — Skill model + rigorous validation: Theokit-compatible SKILL.md frontmatter parser
  (`yaml`/eemeli), zip-safety validation (`yauzl`; traversal/symlink/zip-bomb/depth/dup/SKILL.md-root)
  + secret scan (`secretlint`), immutable revisions (bytea + sha256 + jsonb), full CRUD
  (paginated list, updateMask, delete + configurable id reservation, revisions endpoints).

### Changed
- Frontmatter YAML parser switched gray-matter → `yaml` (eemeli) to avoid the js-yaml 3.x CVE.

### Fixed
- A deleted skill_id can be recreated after the reservation window expires (BLOCKER from /review).
- Index on `skill_revisions(skill_id, create_time desc)`.

### Security
- Inbound body-size limit (413) on POST/PATCH — memory-DoS guard (`THEOSKILL_MAX_BODY_BYTES`).
