# Release v0.3.0

**Date:** 2026-06-23
**Verdict:** RELEASED
**Milestone:** M2 — LRO robusto + governança + webhook
**Source review:** knowledge-base/reviews/m2-lro-governance-webhook-review-2026-06-23.md
**PR:** https://github.com/usetheodev/theo-skillregistry/pull/3
**Merge commit:** 66bfb6c99d848e0fab8930ae33667cd3f7d69da3
**Tag:** v0.3.0 (annotated)
**GitHub release:** https://github.com/usetheodev/theo-skillregistry/releases/tag/v0.3.0

## Release notes

### Added
- M2: ciclo de vida explícito de operações LRO (`CREATING`/`UPDATING`/`DELETING` →
  `ACTIVE`/`FAILED`) com idempotência via header `Idempotency-Key` e classificação de
  retry (regra de negócio = sem retry → `FAILED`; transiente = retry com backoff) (#5)
- M2: primitivos de segurança de webhook — SSRF guard (`assertPublicUrl`), assinatura
  HMAC-SHA256 (esquema Inngest `t=<ts>&s=<hex>`, janela de replay ±5min) e sender HTTP
  com IP-pinning em tempo de entrega (`node:http(s)`) (#5)
- M2: CRUD de webhook endpoints (`/v1/webhookEndpoints`) — segredo HMAC gerado pelo
  servidor e retornado uma única vez na criação; URL validada via SSRF guard; filtro
  opcional por `event_types` (#5)
- M2: pipeline de entrega de webhooks com garantias — fan-out transacional via outbox,
  worker de entrega com classificação de retry (2xx=entregue / 3xx-4xx=falha permanente /
  5xx=retry com backoff → dead-letter), reconciler de órfãos + stuck via
  `FOR UPDATE SKIP LOCKED` e dedup por `singletonKey`/`webhook-id` (at-least-once) (#5)

## Review summary

`/review` = READY_TO_MERGE. 5 agentes especialistas + re-verificação adversarial do BLOCKER.
1 BLOCKER (SSRF em tempo de entrega) fechado e re-verificado; 8 HIGH corrigidos/mitigados.
Gates: typecheck PASS, lint 0/0, code-quality PASS (cap 100), 97 testes verdes. Zero deps novas.
