# Release v0.4.0

**Date:** 2026-06-23
**Verdict:** RELEASED
**Milestone:** M3 — Embeddings plugáveis + indexação (pgvector)
**Source review:** knowledge-base/reviews/m3-embeddings-pgvector-review-2026-06-23.md
**PR:** https://github.com/usetheodev/theo-skillregistry/pull/4
**Merge commit:** 0c46bd8fb51bbd700cd9a4320aa28ef29bfdd29a
**Tag:** v0.4.0 (annotated)
**GitHub release:** https://github.com/usetheodev/theo-skillregistry/releases/tag/v0.4.0

## Release notes

### Added
- M3: porta `EmbeddingProvider` (DIP) com adapters `stub` (determinístico) e `openai` (SDK;
  `local` = mesmo adapter com `OPENAI_BASE_URL`); dimensão pinada em 1536 com guard fail-fast (#6)
- M3: schema pgvector — `vector(1536)` + tabela `embeddings` (por revisão; unique
  `(revision_id, provider, model)`; índice HNSW cosine) + coluna `skill_revisions.skill_md`;
  extensão `vector` no bootstrap da migração (#6)
- M3: geração e indexação assíncrona — seleção de provider por env; job `embed_skill` chaveado por
  revisão (cada revisão embeddada exatamente uma vez; update nunca dedupa contra a anterior);
  embed worker valida a dimensão (fail-fast) e faz upsert idempotente; dead-letter observável;
  busca por similaridade cosseno consultável (#6)

## Review summary

`/review` = READY_TO_MERGE. 4 agentes especialistas (architecture, tests, domain-embeddings/pgvector,
cross-validation). 0 BLOCKERs; o único defeito de correção (dedup por skill_id deixava a nova revisão
sem embedding) foi pego pelo teste de UPDATE do milestone e corrigido (chaveamento por revision_id).
HIGH/MEDIUM corrigidos. Gates: typecheck PASS, lint 0/0, code-quality PASS (cap 100), deps-audit 0 CVEs,
134 testes verdes.
