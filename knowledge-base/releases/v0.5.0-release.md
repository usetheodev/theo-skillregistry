# Release v0.5.0

**Date:** 2026-06-23
**Verdict:** RELEASED
**Milestone:** M4 — Busca híbrida transparente (RetrieveSkills)
**Source review:** knowledge-base/reviews/m4-hybrid-retrieve-review-2026-06-23.md
**PR:** https://github.com/usetheodev/theo-skillregistry/pull/5
**Merge commit:** 91e0b0d3066cca39416e291503675eb3c1e746fa
**Tag:** v0.5.0 (annotated)
**GitHub release:** https://github.com/usetheodev/theo-skillregistry/releases/tag/v0.5.0

## Release notes

### Added
- M4: índice de busca lexical (`skills.search_text` + `search_tsv` gerado + GIN) mantido síncrono (#7)
- M4: porta `SkillRetriever` (DIP) com adapters vector (cosine pgvector), keyword (FTS lexemas
  OR-ados via `to_tsquery` + `ts_rank`) e hybrid (RRF k=60 calibration-free sobre pool profundo,
  degradação graciosa de ambos os lados); `ParamBuilder` seguro (#7)
- M4: endpoint `GET /v1/skills:retrieve` — híbrido por default, `score` explícito + `trace_id`;
  dispatcher por strategy; métrica `retrieve` (latency_ms + top_score) — north-star (#7)
- M4: eval set versionado — Recall@5 ≥ 0.85 e p95 < 200ms medidos e reproduzíveis (recall via FTS
  lexical com stub; OpenAI adiciona semântico em produção) (#7)

## Review summary

`/review` = READY_TO_MERGE. 4 agentes especialistas (architecture, tests, domain-retrieval/FTS/RRF,
cross-validation). 0 BLOCKERs. HIGH/MEDIUM corrigidos com foco em honestidade (Rule 3): CHANGELOG
descreve a query OR-lexema real; gate de recall significativo; teste expõe a perna vetorial inerte
sob stub; pool de fusão profundo; degradação de ambos os lados; `RetrieverError` wired. "rerank" do
DoD = RRF reordena; cross-encoder deferido. Gates: typecheck PASS, lint 0/0, code-quality PASS
(cap 100), deps-audit zero deps novas, 172 testes verdes.
